<h1>Listing teams</h1>

<table>
  <tr>
    <th>Team name</th>
    <th>Team img</th>
  </tr>

<% @teams.each do |team| %>
  <tr>
    <td><%=h team.team_name %></td>
    <td><%=h team.team_img %></td>
    <td><%= link_to 'Show', team %></td>
    <td><%= link_to 'Edit', edit_team_path(team) %></td>
    <td><%= link_to 'Destroy', team, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New team', new_team_path %>